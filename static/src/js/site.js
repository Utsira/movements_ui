/* global window */
window.jQuery = window.$ = require('jquery');
const $ = window.$;

const React = require('react')
const ReactDOM = require('react-dom');
const App = require('./home.js');
//ReactDOM.render(<App/>, document.body);